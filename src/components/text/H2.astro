---

const { color = 'blue', class: className, ...rest } = Astro.props;

const colorStyle = `text-${color} `;
const styles = colorStyle + 'font-semibold text-23 md:text-28 lg:text-36' + (className ? " " + className : "");

---

<h2 class={styles} {...rest}>
  <slot />
</h2>
